const expect = require('chai').expect();

describe('Sample Test Suite', function() {
	it('Sample synchronous test case', function() {
		//expect(false).equal(true);
	})

	it('Sample A-synchronous test case', function(done) {
		done();
		// done('error')
	})
})